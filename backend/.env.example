PORT=4000
DB_PATH=./data/taller.db
UPLOADS_DIR=./storage/uploads

# Webhooks de ingesta (WhatsApp / email). Desactivados por defecto en la demo.
# Ponlo a 1 solo si quieres montar /api/webhooks/* (ver README, sección Seguridad).
WEBHOOKS_ENABLED=0
